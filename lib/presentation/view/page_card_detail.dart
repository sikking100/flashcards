import 'package:flutter/material.dart';

class PageCardDetail extends StatelessWidget {
  const PageCardDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
