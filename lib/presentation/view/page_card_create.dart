import 'package:flutter/material.dart';

class PageCardCreate extends StatelessWidget {
  const PageCardCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
